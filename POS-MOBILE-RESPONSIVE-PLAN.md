# خطة تحويل نظام الكاشير (QPOS) إلى تصميم موبايل ريسبونسف كامل
# QPOS Mobile-First Responsive Redesign Plan

---

## 📊 ملخص الفحص الشامل | Comprehensive Audit Summary

### الملفات التي تم فحصها:

| # | الملف | الحجم | الوصف |
|---|-------|-------|-------|
| 1 | `app/pos/qpos/page.tsx` | ~2000+ سطر | الصفحة الرئيسية للكاشير - ملف ضخم واحد |
| 2 | `app/pos/receipt/page.tsx` | ~200 سطر | صفحة عرض الفاتورة ✅ (ريسبونسف بالفعل) |
| 3 | `components/barcode-scanner.tsx` | ~250 سطر | ماسح الباركود ✅ (ريسبونسف بالفعل) |
| 4 | `lib/actions/pos.ts` | ~1200 سطر | الأكشنز الأساسية (منتجات، مبيعات، PIN) |
| 5 | `lib/actions/pos-features.ts` | ~600 سطر | الميزات المتقدمة (عملاء، مرتجعات، ورديات) |
| 6 | `lib/actions/pos-pharmacy.ts` | ~1800 سطر | وحدة الصيدلية (صلاحية، روشتات، تأمين) |
| 7 | `lib/actions/pos-clothing.ts` | ~900 سطر | وحدة الملابس (مقاسات، ولاء، تقسيط) |
| 8 | `lib/stores/cart-store.ts` | ~75 سطر | حالة السلة (Zustand) |

---

## 🔴 المشاكل الحالية على الموبايل | Current Mobile Issues

### مشاكل حرجة (Critical)

1. **ملف واحد ضخم 2000+ سطر** - صعب الصيانة والتحسين
   - 70+ متغير حالة `useState`
   - 30+ دالة `useCallback`
   - كل شيء في مكون واحد

2. **الهيدر مزدحم جداً على الموبايل**
   - 15+ زر في الهيدر (وردية، مرتجعات، صلاحية، روشتات، تأمين، مريض، باتش، FEFO، ولاء، تقسيط، موسم، موردين، هدايا، حجز، خصم فاتورة، ملخص، سجل مبيعات)
   - الأزرار تخفي النص وتبقي الأيقونات فقط على sm، لكنها لا تزال كثيرة

3. **بطاقات المنتجات مزدحمة على شاشات صغيرة**
   - `grid-cols-3` على الموبايل → الأزرار (تعديل، إضافة كمية، دوائي، بدائل، ملصق، وحدات، مقاسات، حجز، سعر) تكون صغيرة جداً
   - المحتوى يتراكم ويصبح غير قابل للقراءة

4. **السلة الجانبية لا تعمل بشكل مثالي**
   - `w-[85vw]` على الموبايل جيد لكن التفاعل مع عناصر السلة ضيق
   - أزرار الكمية (+/-) صغيرة = 28px فقط (يجب أن تكون 44px للمس)

5. **المودالات الكثيرة (~25 مودال) ليست محسنة للموبايل**
   - لا يوجد `max-height` مع scroll
   - بعضها يحتوي نماذج طويلة بدون تصميم موبايل
   - لا يوجد تصميم bottom-sheet للموبايل

### مشاكل متوسطة (Medium)

6. **عدم وجود Bottom Navigation** - المستخدم يحتاج تمرير للأعلى للوصول لأي زر
7. **البحث لا يدعم debounce** - ضغط على السيرفر
8. **لا يوجد تحسين للمحادثات على المس** - swipe gestures غير موجودة
9. **خط صغير في بعض الأماكن** - `text-[8px]` و `text-[10px]` صعب القراءة على الموبايل
10. **الـ overlay (bg-black/50)** للسلة لا يغطي الهيدر

### مشاكل طفيفة (Low)

11. **لا يوجد أنيميشن سلس** للتنقل بين الأقسام
12. **عدم وجود haptic feedback** عند الإضافة للسلة
13. **لا يوجد pull-to-refresh** لتحديث المنتجات

---

## 🏗️ خطة التنفيذ | Implementation Plan

### المرحلة 1: تقسيم الملف الضخم (Critical Refactoring)
> **الهدف**: تحويل ملف 2000+ سطر إلى مكونات قابلة للصيانة

#### 1.1 إنشاء هيكل المجلدات الجديد
```
app/pos/qpos/
├── page.tsx                          ← Entry point (~100 سطر)
├── _components/
│   ├── pos-provider.tsx              ← Context + State Management
│   ├── pos-header.tsx                ← الهيدر الرئيسي
│   ├── pos-header-actions.tsx        ← أزرار الهيدر (القائمة المنسدلة)
│   ├── pos-search-bar.tsx            ← شريط البحث والفلاتر
│   ├── pos-product-grid.tsx          ← شبكة المنتجات
│   ├── pos-product-card.tsx          ← بطاقة المنتج الواحدة
│   ├── pos-cart-panel.tsx            ← لوحة السلة
│   ├── pos-cart-item.tsx             ← عنصر في السلة
│   ├── pos-bottom-bar.tsx            ← الشريط السفلي (موبايل)
│   ├── pos-mobile-actions-sheet.tsx  ← Bottom sheet للأدوات
│   ├── pos-payment-modal.tsx         ← مودال الدفع
│   ├── pos-receipt-modal.tsx         ← مودال الفاتورة
│   ├── pos-pin-modal.tsx             ← مودال PIN
│   ├── pos-quick-add-modal.tsx       ← مودال إضافة منتج سريع
│   ├── pos-edit-product-modal.tsx    ← مودال تعديل المنتج
│   ├── pos-history-modal.tsx         ← مودال سجل المبيعات
│   ├── pos-summary-modal.tsx         ← مودال ملخص اليوم
│   ├── pos-customer-modal.tsx        ← مودال العملاء
│   ├── pos-return-modal.tsx          ← مودال المرتجعات
│   ├── pos-shift-modal.tsx           ← مودال الورديات
│   ├── pos-coupon-modal.tsx          ← مودال الكوبونات
│   ├── pos-held-carts-modal.tsx      ← مودال السلات المعلقة
│   ├── pharmacy/
│   │   ├── pos-expiry-alerts.tsx
│   │   ├── pos-prescription-modal.tsx
│   │   ├── pos-insurance-modal.tsx
│   │   ├── pos-patient-modal.tsx
│   │   ├── pos-drug-label-modal.tsx
│   │   ├── pos-batch-inventory.tsx
│   │   ├── pos-fefo-modal.tsx
│   │   ├── pos-unit-modal.tsx
│   │   └── pos-alternatives-modal.tsx
│   └── clothing/
│       ├── pos-variant-modal.tsx
│       ├── pos-loyalty-modal.tsx
│       ├── pos-installment-modal.tsx
│       ├── pos-season-modal.tsx
│       ├── pos-supplier-modal.tsx
│       ├── pos-gift-card-modal.tsx
│       ├── pos-reservation-modal.tsx
│       └── pos-invoice-discount-modal.tsx
├── _hooks/
│   ├── use-pos-state.ts              ← useReducer بدل 70 useState
│   ├── use-pos-products.ts           ← تحميل وفلترة المنتجات
│   ├── use-pos-cart.ts               ← عمليات السلة
│   ├── use-pos-keyboard.ts           ← اختصارات الكيبورد
│   ├── use-pos-pharmacy.ts           ← حالة الصيدلية
│   └── use-pos-clothing.ts           ← حالة الملابس
└── _types/
    └── pos-types.ts                  ← كل الأنواع في مكان واحد
```

#### 1.2 تحويل 70+ useState إلى useReducer
```typescript
// قبل (الحالة الحالية):
const [showPayment, setShowPayment] = useState(false)
const [showReceipt, setShowReceipt] = useState(false)
const [showHistory, setShowHistory] = useState(false)
// ... 67 متغير آخر

// بعد (التحسين):
type POSState = {
  activeModal: 'payment' | 'receipt' | 'history' | 'summary' | 'pin' | ... | null
  cart: CartItem[]
  search: { query: string; category: string }
  discount: { value: string; type: 'percentage' | 'fixed' }
  payment: { method: PaymentMethod; amountPaid: string }
  customer: { name: string; phone: string; selected: POSCustomer | null }
  pharmacy: PharmacyState
  clothing: ClothingState
  ui: { error: string; success: string; loading: boolean }
}

const [state, dispatch] = useReducer(posReducer, initialState)
```

---

### المرحلة 2: إعادة تصميم الواجهة للموبايل (Mobile-First UI Redesign)

#### 2.1 الهيدر الجديد - Header Redesign

**الحالة الحالية**: 15+ زر مضغوط في صف واحد
**التصميم الجديد**:

```
┌─────────────────────────────────────┐ 
│ [<]  اسم المتجر   [🛒 3] [☰ المزيد] │  ← الموبايل: 3 عناصر فقط
└─────────────────────────────────────┘
```

- **موبايل (< 768px)**:
  - زر رجوع + اسم المتجر + أيقونة السلة مع العداد + زر "المزيد" (☰)
  - زر "المزيد" يفتح Bottom Sheet بكل الأدوات مرتبة في grid 3×N
  
- **تابلت (768px - 1024px)**:
  - اسم المتجر + أزرار الأدوات الأساسية (وردية، مرتجعات، ملخص، سجل) + زر "المزيد"
  
- **ديسكتوب (> 1024px)**:
  - التصميم الحالي مع كل الأزرار ظاهرة

#### 2.2 Bottom Sheet للأدوات (Mobile Actions)

```
┌───────────────────────────────────────┐
│  ─────────  (drag handle)             │
│                                       │
│  🔄 وردية    📦 مرتجعات    📋 سجل    │
│  📊 ملخص     🏷️ كوبون     👤 عملاء   │
│                                       │
│  ── أدوات الصيدلية ──                 │
│  ⚠️ صلاحية   📝 روشتات    🏥 تأمين   │
│  👨‍⚕️ مريض    📦 باتش      🔄 FEFO    │
│                                       │
│  ── أدوات الملابس ──                  │
│  📏 مقاسات   ⭐ ولاء      💳 تقسيط   │
│  🌡️ موسم    🚚 موردين    🎁 هدايا   │
│  📌 حجز     💰 خصم فاتورة            │
└───────────────────────────────────────┘
```

#### 2.3 شريط البحث - Search Bar Redesign

**الحالة الحالية**: بحث + زر كاميرا + زر إضافة منتج في صف واحد
**التصميم الجديد**:

```
موبايل:
┌─────────────────────────────────────┐
│ 🔍 بحث بالاسم أو الباركود...  [📷] │  ← صف واحد مبسط
├─────────────────────────────────────┤
│ [الكل] [فئة 1] [فئة 2] [فئة 3] >> │  ← فلتر الفئات (scrollable)
└─────────────────────────────────────┘
```

- الكاميرا تكون أيقونة داخل حقل البحث أو بجانبه
- زر إضافة المنتج ينتقل إلى Bottom Sheet الأدوات
- ارتفاع حقل البحث يزيد إلى `h-12` (48px) لسهولة اللمس
- فلتر الفئات يبقى scrollable أفقياً مع `scroll-snap`

#### 2.4 شبكة المنتجات - Product Grid Redesign

**الحالة الحالية**: `grid-cols-3` مع 6+ أزرار لكل منتج
**التصميم الجديد**:

```
موبايل (< 640px):
┌──────────────────┬──────────────────┐
│  ┌────────────┐  │  ┌────────────┐  │
│  │   صورة     │  │  │   صورة     │  │
│  │            │  │  │            │  │
│  ├────────────┤  │  ├────────────┤  │
│  │ اسم المنتج │  │  │ اسم المنتج │  │
│  │ 10.50 ج  5 │  │  │ 25.00 ج 12 │  │
│  │ [+إضافة]   │  │  │ [+إضافة]   │  │
│  └────────────┘  │  └────────────┘  │
└──────────────────┴──────────────────┘

// Long press أو زر ⋮ يفتح قائمة الخيارات
```

**التغييرات**:
- `grid-cols-2` على الموبايل بدلاً من `grid-cols-3`
- إزالة كل الأزرار (تعديل، كمية، دوائي...) من البطاقة
- إضافة زر واحد فقط للإضافة للسلة
- **Long Press** أو زر ⋮ يفتح context menu بالخيارات:
  ```
  ┌─────────────────────┐
  │ ✏️ تعديل المنتج     │
  │ 📦 إضافة كمية       │
  │ 💊 بيانات دوائية    │  ← صيدلية فقط
  │ 🔬 بدائل           │  ← صيدلية فقط
  │ 🏷️ ملصق           │  ← صيدلية فقط
  │ 📐 وحدات قياس      │  ← صيدلية فقط
  │ 📏 مقاسات/ألوان    │  ← ملابس فقط
  │ 📌 حجز             │  ← ملابس فقط
  │ 🏷️ تاج سعر        │  ← ملابس فقط
  └─────────────────────┘
  ```

- **تابلت (640px - 1024px)**: `grid-cols-3` مع أزرار أساسية فقط (تعديل + كمية)
- **ديسكتوب (> 1024px)**: `grid-cols-4` أو `grid-cols-5` مع كل الأزرار

#### 2.5 لوحة السلة - Cart Panel Redesign

**الحالة الحالية**: سلة جانبية ثابتة على الديسكتوب + overlay على الموبايل
**التصميم الجديد**:

```
موبايل - Bottom Bar:
┌─────────────────────────────────────┐
│   المنتجات (المحتوى الرئيسي)       │
│                                     │
│                                     │
│                                     │
├─────────────────────────────────────┤
│ 🛒 3 منتجات  |  156.50 ج  | [دفع→] │  ← شريط سفلي ثابت
└─────────────────────────────────────┘

// الضغط على الشريط يفتح السلة كاملة (Full-screen slide up)

موبايل - السلة المفتوحة:
┌─────────────────────────────────────┐
│  ─────────  (drag handle)           │
│  🛒 السلة (3)            [X إغلاق] │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐    │
│  │ [صورة] منتج 1    [-] 2 [+] │    │
│  │         10.50 ج    = 21.00  │    │
│  └─────────────────────────────┘    │
│                                     │
│  ┌─────────────────────────────┐    │
│  │ [صورة] منتج 2    [-] 1 [+] │    │
│  │         50.00 ج    = 50.00  │    │
│  └─────────────────────────────┘    │
│                                     │
├─────────────────────────────────────┤
│ 🏷️ خصم: [____] [%|ج]              │
│ 🎟️ كود خصم: [__________] [تطبيق]  │
├─────────────────────────────────────┤
│ المجموع:              71.00 ج      │
│ الخصم:                -5.00 ج      │
│ ────────────────────────────        │
│ الإجمالي:             66.00 ج      │
│                                     │
│ [        💰 دفع الآن (F9)        ]  │  ← زر كبير 56px
└─────────────────────────────────────┘
```

**عناصر السلة المحسنة**:
- أزرار +/- بحجم 44px (touch target)
- Swipe left to delete (سحب يسار لحذف)
- أنيميشن سلسة عند الإضافة/الحذف

#### 2.6 الشريط السفلي - Bottom Navigation Bar (موبايل فقط)

```
┌─────────────────────────────────────┐
│ [🏠 المنتجات] [🛒 السلة 3] [⚙️ أدوات] │
└─────────────────────────────────────┘
```

- يظهر فقط على شاشات < 768px
- **المنتجات**: العرض الرئيسي
- **السلة**: يفتح السلة مع العداد
- **أدوات**: يفتح Bottom Sheet بكل الأدوات

#### 2.7 المودالات - Modal Redesign

**الحالة الحالية**: مودالات ثابتة في المنتصف بدون تحسين للموبايل
**التصميم الجديد**:

```
موبايل (< 768px):
- المودالات تفتح كـ Full-screen page (slide from bottom)
- هيدر ثابت مع زر إغلاق
- محتوى scrollable
- أزرار الإجراء في الأسفل (sticky)

تابلت (768px - 1024px):
- Bottom Sheet بارتفاع 75% من الشاشة
- قابل للسحب للأسفل للإغلاق

ديسكتوب (> 1024px):
- Centered modal كالحالي
```

**مكون مودال موحد**:
```typescript
// components/ui/responsive-modal.tsx
interface ResponsiveModalProps {
  open: boolean
  onClose: () => void
  title: string
  children: React.ReactNode
  footer?: React.ReactNode
}

// يتكيف تلقائياً مع حجم الشاشة:
// - موبايل: full-screen slide-up
// - تابلت: bottom sheet 75%
// - ديسكتوب: centered dialog
```

#### 2.8 مودال الدفع - Payment Modal Redesign

```
موبايل (Full-screen):
┌─────────────────────────────────────┐
│  ← رجوع          الدفع             │
├─────────────────────────────────────┤
│                                     │
│  الإجمالي: 66.00 جنيه              │
│                                     │
│  ── طريقة الدفع ──                  │
│  ┌─────┐ ┌─────┐ ┌─────┐          │
│  │ 💵  │ │ 💳  │ │ 👛  │          │
│  │نقدي │ │بطاقة│ │محفظة│          │
│  └─────┘ └─────┘ └─────┘          │
│                                     │
│  المبلغ المدفوع:                    │
│  ┌─────────────────────────┐       │
│  │         66.00           │       │  ← حقل كبير
│  └─────────────────────────┘       │
│                                     │
│  الباقي: 0.00 جنيه                 │
│                                     │
│  ── أزرار سريعة ──                  │
│  [50] [100] [200] [المبلغ بالضبط]  │
│                                     │
│  👤 العميل (اختياري):               │
│  [___________] [🔍]                │
│                                     │
├─────────────────────────────────────┤
│ [        ✅ إتمام البيع           ] │  ← sticky bottom
└─────────────────────────────────────┘
```

---

### المرحلة 3: تحسين الأداء | Performance Optimization

#### 3.1 تحسين الرندر
```typescript
// إضافة React.memo لكل بطاقة منتج
const ProductCard = React.memo(({ product, onAdd, onAction }: Props) => {
  // ...
})

// إضافة debounce للبحث
const debouncedSearch = useDeferredValue(searchQuery)
// أو
const debouncedSearch = useDebounce(searchQuery, 300)
```

#### 3.2 Virtual Scrolling للمنتجات
```typescript
// استخدام react-window أو @tanstack/react-virtual
// لعرض المنتجات الظاهرة فقط (مهم لو 100+ منتج)
import { useVirtualizer } from '@tanstack/react-virtual'
```

#### 3.3 تحميل كسول للمودالات
```typescript
// كل مودال يتحمل فقط عند الحاجة
const PaymentModal = dynamic(() => import('./_components/pos-payment-modal'))
const HistoryModal = dynamic(() => import('./_components/pos-history-modal'))
const PharmacyModals = dynamic(() => import('./_components/pharmacy/pos-pharmacy-modals'))
```

#### 3.4 تحسين الصور
```typescript
// استخدام next/image بدلاً من <img> العادية
import Image from 'next/image'

<Image
  src={product.image_url}
  alt={product.name}
  width={200}
  height={200}
  className="object-cover"
  loading="lazy"
  placeholder="blur"
/>
```

---

### المرحلة 4: تحسينات UX للموبايل | Mobile UX Enhancements

#### 4.1 Touch Targets
```css
/* كل العناصر التفاعلية >= 44px */
.touch-target {
  min-height: 44px;
  min-width: 44px;
}
```

#### 4.2 Swipe Gestures
- **السلة**: سحب يسار لحذف عنصر
- **المودالات**: سحب أسفل للإغلاق
- **المنتجات**: Long press لقائمة الخيارات

#### 4.3 أنيميشن سلسة
```typescript
// Framer Motion للأنيميشن
import { motion, AnimatePresence } from 'framer-motion'

// أنيميشن إضافة للسلة
<motion.div
  initial={{ scale: 0.8, opacity: 0 }}
  animate={{ scale: 1, opacity: 1 }}
  exit={{ scale: 0.8, opacity: 0 }}
>
```

#### 4.4 Safe Area Support (للوحات مع notch)
```css
.bottom-bar {
  padding-bottom: env(safe-area-inset-bottom, 0px);
}

.header {
  padding-top: env(safe-area-inset-top, 0px);
}
```

#### 4.5 منع iOS Zoom على الـ Input
```css
/* منع الزووم التلقائي على iOS */
input, select, textarea {
  font-size: 16px; /* يمنع iOS من عمل zoom */
}
```

---

### المرحلة 5: CSS Breakpoints System

```css
/* breakpoints مقترحة */
/* xs: 0-374px    - هواتف صغيرة */
/* sm: 375-639px  - هواتف عادية */
/* md: 640-767px  - هواتف كبيرة/تابلت صغير */
/* lg: 768-1023px - تابلت */
/* xl: 1024px+    - ديسكتوب */

/* Mobile First Approach */
.container {
  /* Base: Mobile */
  padding: 8px;
  
  /* Tablet */
  @screen md { padding: 16px; }
  
  /* Desktop */  
  @screen lg { padding: 24px; }
}
```

---

## 📋 ترتيب التنفيذ | Execution Priority

### الأسبوع 1: الأساسيات
| # | المهمة | الأولوية | التأثير |
|---|--------|----------|---------|
| 1 | إنشاء `pos-types.ts` - نقل كل الأنواع | 🔴 عالية | أساس التقسيم |
| 2 | إنشاء `use-pos-state.ts` - useReducer | 🔴 عالية | تنظيم الحالة |
| 3 | إنشاء `pos-provider.tsx` - Context | 🔴 عالية | مشاركة الحالة |
| 4 | فصل `pos-header.tsx` | 🔴 عالية | تبسيط الملف |
| 5 | فصل `pos-product-card.tsx` | 🔴 عالية | تقليل الرندر |
| 6 | فصل `pos-cart-panel.tsx` | 🔴 عالية | تحسين السلة |

### الأسبوع 2: التصميم الموبايل
| # | المهمة | الأولوية | التأثير |
|---|--------|----------|---------|
| 7 | إنشاء `pos-bottom-bar.tsx` | 🔴 عالية | تجربة الموبايل |
| 8 | إنشاء `pos-mobile-actions-sheet.tsx` | 🔴 عالية | تنظيم الأدوات |
| 9 | إنشاء `responsive-modal.tsx` المكون الموحد | 🔴 عالية | كل المودالات |
| 10 | تحسين شبكة المنتجات (grid-cols-2 mobile) | 🟠 متوسطة | عرض أفضل |
| 11 | تحسين بطاقة المنتج (long press menu) | 🟠 متوسطة | أزرار أقل |
| 12 | تحسين عناصر السلة (44px touch, swipe) | 🟠 متوسطة | سهولة الاستخدام |

### الأسبوع 3: المودالات والتفاصيل
| # | المهمة | الأولوية | التأثير |
|---|--------|----------|---------|
| 13 | فصل مودال الدفع | 🟠 متوسطة | تنظيم الكود |
| 14 | فصل مودال السجل | 🟠 متوسطة | تنظيم الكود |
| 15 | فصل مودالات الصيدلية (6 مودالات) | 🟠 متوسطة | تنظيم الكود |
| 16 | فصل مودالات الملابس (7 مودالات) | 🟠 متوسطة | تنظيم الكود |
| 17 | إضافة debounce للبحث | 🟡 منخفضة | أداء |
| 18 | إضافة virtual scrolling | 🟡 منخفضة | أداء |

### الأسبوع 4: التلميع والاختبار
| # | المهمة | الأولوية | التأثير |
|---|--------|----------|---------|
| 19 | إضافة أنيميشن (framer-motion) | 🟡 منخفضة | تجربة أفضل |
| 20 | Safe Area + iOS fixes | 🟡 منخفضة | توافقية |
| 21 | اختبار على أجهزة حقيقية | 🔴 عالية | ضمان الجودة |
| 22 | اختبار RTL على الموبايل | 🔴 عالية | دعم العربية |
| 23 | Performance audit (Lighthouse) | 🟠 متوسطة | أداء |
| 24 | تحسين الصور (next/image) | 🟡 منخفضة | سرعة التحميل |

---

## 🎨 مخطط UI المقترح | Proposed UI Wireframes

### الموبايل - الشاشة الرئيسية
```
┌─────────────────────────────────────┐
│ [<] اسم المتجر            [☰ المزيد]│  ← Header (56px)
├─────────────────────────────────────┤
│ 🔍 بحث بالاسم أو الباركود... [📷]  │  ← Search (48px)
│ [الكل][فئة 1][فئة 2][فئة 3]  >>   │  ← Categories (40px)
├─────────────────────────────────────┤
│                                     │
│  ┌────────┐  ┌────────┐            │
│  │  صورة  │  │  صورة  │            │  ← Products Grid
│  │        │  │        │            │    (2 columns)
│  │ اسم    │  │ اسم    │            │
│  │ سعر  ك │  │ سعر  ك │            │
│  └────────┘  └────────┘            │
│                                     │
│  ┌────────┐  ┌────────┐            │
│  │  صورة  │  │  صورة  │            │
│  │        │  │        │            │
│  │ اسم    │  │ اسم    │            │
│  │ سعر  ك │  │ سعر  ك │            │
│  └────────┘  └────────┘            │
│                                     │
├─────────────────────────────────────┤
│ 🛒 3 عناصر │ 156.50 ج │  [دفع →]  │  ← Cart Bar (60px)
├─────────────────────────────────────┤
│ [🏠 منتجات] [🛒 السلة] [⚙️ أدوات] │  ← Bottom Nav (56px)
└─────────────────────────────────────┘
```

### الموبايل - السلة مفتوحة
```
┌─────────────────────────────────────┐
│  ─────────  (drag to close)         │
│  🛒 السلة (3 عناصر)      [X]       │
├─────────────────────────────────────┤
│                                     │
│  ← swipe لحذف                      │
│  ┌──────────────────────────────┐   │
│  │ [📷] منتج 1                 │   │
│  │      10.50 ج   [-] 2 [+]   │   │
│  │                 = 21.00 ج   │   │
│  └──────────────────────────────┘   │
│                                     │
│  ┌──────────────────────────────┐   │
│  │ [📷] منتج 2                 │   │
│  │      50.00 ج   [-] 1 [+]   │   │
│  │                 = 50.00 ج   │   │
│  └──────────────────────────────┘   │
│                                     │
│  ┌──────────────────────────────┐   │
│  │ [📷] دواء 3                 │   │
│  │      25.00 ج   [-] 3 [+]   │   │
│  │      [شريط]     = 75.00 ج  │   │
│  └──────────────────────────────┘   │
│                                     │
├─────────────────────────────────────┤
│ 🏷️ خصم [____] [%|ج]               │
│ 🎟️ كود [__________] [✓]           │
├─────────────────────────────────────┤
│ المجموع:           146.00 ج        │
│ الخصم:             -10.00 ج        │
│ ─────────────────────────           │
│ الإجمالي:          136.00 ج        │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │       💰 دفع 136.00 جنيه       │ │  ← Big CTA (56px)
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

### التابلت - الشاشة الرئيسية
```
┌──────────────────────────────────────────────────────┐
│ [<] اسم المتجر  [وردية][مرتجعات][ملخص][سجل] [☰]   │
├──────────────────────────────────────────────────────┤
│ 🔍 بحث...  [📷] [+ منتج]                            │
│ [الكل][فئة 1][فئة 2][فئة 3][فئة 4] >>              │
├──────────────────────────────┬───────────────────────┤
│                              │ 🛒 السلة (3)         │
│ ┌──────┐ ┌──────┐ ┌──────┐ │                       │
│ │صورة  │ │صورة  │ │صورة  │ │ منتج 1  [-]2[+] 21ج │
│ │اسم   │ │اسم   │ │اسم   │ │ منتج 2  [-]1[+] 50ج │
│ │سعر ك │ │سعر ك │ │سعر ك │ │ منتج 3  [-]3[+] 75ج │
│ └──────┘ └──────┘ └──────┘ │                       │
│                              │ ─────────────────── │
│ ┌──────┐ ┌──────┐ ┌──────┐ │ الإجمالي: 136.00 ج  │
│ │صورة  │ │صورة  │ │صورة  │ │                       │
│ │اسم   │ │اسم   │ │اسم   │ │ [   💰 دفع الآن   ] │
│ │سعر ك │ │سعر ك │ │سعر ك │ │                       │
│ └──────┘ └──────┘ └──────┘ │                       │
└──────────────────────────────┴───────────────────────┘
```

---

## 🔧 التقنيات المطلوبة | Required Technologies

| التقنية | الاستخدام | الحالة |
|---------|----------|--------|
| Tailwind CSS | التصميم الحالي + responsive classes | ✅ موجود |
| Framer Motion | أنيميشن سلسة | 🔲 يحتاج تثبيت |
| @tanstack/react-virtual | Virtual scrolling للمنتجات | 🔲 يحتاج تثبيت |
| use-gesture | Swipe/Long press gestures | 🔲 اختياري |
| next/image | تحسين الصور | ✅ موجود |
| React.memo | تقليل إعادة الرندر | ✅ موجود |
| useReducer | إدارة الحالة المعقدة | ✅ موجود |
| dynamic import | تحميل كسول للمكونات | ✅ موجود |

---

## ✅ ما يعمل بشكل جيد حالياً | What Already Works Well

1. ✅ **صفحة الفاتورة** (`receipt/page.tsx`) - تصميم ممتاز ومتجاوب
2. ✅ **ماسح الباركود** - يعمل على كل الأحجام
3. ✅ **دعم RTL** - تبديل اللغة عربي/إنجليزي
4. ✅ **اختصارات الكيبورد** - F2, F9, F12
5. ✅ **السلة على الموبايل** - تظهر كـ overlay (يحتاج تحسين)
6. ✅ **الهيدر** - بعض العناصر تختفي على sm
7. ✅ **أمان السيرفر** - PIN, التحقق من المخزون, المعاملات الذرية
8. ✅ **الأزرار** - لها تأثيرات hover/active/scale

---

## 📱 أجهزة الاختبار المطلوبة | Required Test Devices

| الجهاز | الدقة | الأهمية |
|--------|-------|---------|
| iPhone SE (2nd gen) | 375×667 | 🔴 أصغر شاشة مدعومة |
| iPhone 14 | 390×844 | 🔴 الأكثر شيوعاً |
| iPhone 14 Pro Max | 430×932 | 🟠 شاشة كبيرة |
| Samsung Galaxy S21 | 360×800 | 🔴 Android شائع |
| iPad Mini | 768×1024 | 🟠 تابلت |
| iPad Pro 11" | 834×1194 | 🟡 تابلت كبير |

---

## 📝 ملاحظات مهمة | Important Notes

1. **لا تكسر الديسكتوب**: كل التغييرات يجب أن تكون متوافقة مع التصميم الحالي على الديسكتوب
2. **التدرج في التنفيذ**: ابدأ بالمرحلة 1 (تقسيم الكود) ثم انتقل للتصميم
3. **اختبار مستمر**: اختبر على الموبايل بعد كل تغيير
4. **الأداء أولاً**: لا تضف مكتبات ثقيلة بدون حاجة حقيقية
5. **RTL مهم**: كل تصميم جديد يجب أن يعمل في الاتجاهين
6. **الـ server actions آمنة**: لا تحتاج تعديل - المشكلة في الـ UI فقط
7. **localStorage**: السلة محفوظة محلياً - يجب الحفاظ على هذا السلوك
