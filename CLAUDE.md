# CLAUDE.md

**قواعد هذا الريبو تعيش في [AGENTS.md](AGENTS.md). اقرأه قبل أي عمل.**

هذا الملف تحويل مقصود ولا يحمل قواعد خاصة به: Claude Code و OpenCode وأي وكيل آخر يجب أن يقرأوا نفس الكتاب. قاعدة مكتوبة هنا ومش هناك = نسختان ستفترقان.

الحد الأدنى الذي لا يُبدأ عمل قبل معرفته (والتفصيل والأدلة في `AGENTS.md`):

- **البوابة الحاكمة:** `npx tsc --noEmit` + `npm test` — كلاهما نظيف الآن (0 أخطاء / 85 تست ناجح) ويجب أن يبقى كذلك.
- **`npm run lint` أحمر عند خط الأساس** (4 أخطاء + 5 تحذيرات سابقة) — للمقارنة فقط، لا يُصلَح ولا يزيد. §2
- **`npm run build` مسموح، ومطلوب قبل أي دفع إلى `main`** (قرار المالك 2026-08-07) — البوابتان الحاكمتان عمياوان عن أخطاء حدود سيرفر/عميل في Next، وقد أفلت منهما فعلًا خطأ كسر البناء. §2
- **`npm run dev` يبقى مكروهًا** — يعدّل ملفات لوج متتبَّعة في git فيُنتج diff كاذبًا. §2
- **لا يوجد Firebase emulator** ⇒ أي اتصال بقاعدة البيانات هو اتصال بالإنتاج. §3
- **ممنوع:** أي كتابة git، أي أمر git يضيّع عملًا، أي سكربت في `scripts/`، أي `firebase deploy`، قراءة أو طباعة `.env*`، توسيع النطاق. §3
- **قواعد الكود المُلزِمة** (هوية من الجلسة سيرفر-سايد · إعادة حساب كل مبلغ · `runTransaction` لأي مخزون/رصيد · مسار تخزين لا رابط · عربي RTL فقط · tokens الألوان). §4

الأقسام مرقّمة في `AGENTS.md`: §1 المشروع · §2 الـ gates · §3 الممنوعات · §4 قواعد الكود · §5 سياسة التفويض · §6 البنود المفتوحة · §7 أسماء متغيّرات البيئة.
